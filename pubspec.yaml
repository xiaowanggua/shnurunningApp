name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0 # 确保版本号与您的项目兼容
  shared_preferences: ^2.0.0 # 确保版本号
  http: ^1.0.0 # 或最新稳定版
  path_provider: ^2.0.0 # 或最新稳定版
  connectivity_plus: ^5.0.0 # For checking network connectivity (ensure this is a recent, compatible version)
  # flutter_local_notifications: ^X.X.X # 如果要实现系统通知，后续添加

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true